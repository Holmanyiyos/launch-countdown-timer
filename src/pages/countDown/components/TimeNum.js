import React from "react";

const Timenum = (props)=>{

    return(
        <div className="num__container">
            <h3 className="timenum">{props.num}</h3>
        </div>
    )
}
export default Timenum;